[package]
name = "humfmt"
version = "0.1.0"
edition = "2021"
authors = ["MuXolotl"]
description = "Ergonomic human-readable formatting toolkit for Rust (numbers, bytes, duration, etc.)"
license = "MIT"
repository = "https://github.com/MuXolotl/humfmt"
documentation = "https://docs.rs/humfmt"
keywords = ["format", "humanize", "display", "duration", "bytes"]
categories = ["value-formatting", "text-processing"]

[features]
default = ["std", "english"]

std = []
alloc = []

english = []
russian = []
polish = []

chrono = ["dep:chrono"]
time = ["dep:time"]

[dependencies]
chrono = { version = "0.4", optional = true, default-features = false }
time = { version = "0.3", optional = true, default-features = false }